# Optional. Without it the house runs on embedded Postgres (PGLite).
# For a durable Vercel deploy, set a Neon (or other Postgres) URL.
DATABASE_URL=

# Native shells load the live house. Override if the production host changes.
FLOOR_NATIVE_URL=https://floor-house.vercel.app
